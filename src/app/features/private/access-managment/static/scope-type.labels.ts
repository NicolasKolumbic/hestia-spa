import { ScopeType } from "../typings/scope.type";

export const SCOPE_TYPE_LABELS: Record<ScopeType, string> = {
    CLIENT: 'Cliente',
    SITE: 'Sitio',
    ZONE: 'Zona',
    DEVICE: 'Dispositivo',
};