import { HostControl } from './host-control';

describe('HostControl', () => {
  it('should create an instance', () => {
    const directive = new HostControl();
    expect(directive).toBeTruthy();
  });
});
