import { RenderMap } from './render-map';

describe('RenderMap', () => {
  it('should create an instance', () => {
    const directive = new RenderMap();
    expect(directive).toBeTruthy();
  });
});
