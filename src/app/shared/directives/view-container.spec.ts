import { ViewContainer } from './view-container';

describe('ViewContainer', () => {
  it('should create an instance', () => {
    const directive = new ViewContainer();
    expect(directive).toBeTruthy();
  });
});
