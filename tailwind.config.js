/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'media',
    content: [
        "./src/app/**/*.{html,ts}",
    ],
    plugins: [
        // require('tailwindcss-primeui')
    ],
}